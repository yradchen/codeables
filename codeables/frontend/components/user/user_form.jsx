import React from 'react';
import { Link } from 'react-router';

const UserForm = ({ currentUser, logout }) => {
    return <h1>This will one day be the header</h1>

};

export default UserForm;

// A <Link to> /#/signup
// A <Link to> /#/login
